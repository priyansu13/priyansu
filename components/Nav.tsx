'use client';
import { Menu, X } from 'lucide-react'; import { useEffect, useState } from 'react'; import { profile } from '@/data/portfolio';
const links = ['Home', 'About', 'Education', 'Skills', 'Experience', 'Projects', 'Certifications', 'Achievements', 'Resume', 'Contact'];
export function Nav() { const [open,setOpen]=useState(false); const [scrolled,setScrolled]=useState(false); useEffect(()=>{const f=()=>setScrolled(scrollY>20); f(); addEventListener('scroll',f); return()=>removeEventListener('scroll',f)},[]); return <header className={`fixed inset-x-0 top-0 z-50 transition ${scrolled ? 'border-b border-slate-200/60 bg-white/80 shadow-sm backdrop-blur-xl dark:border-white/10 dark:bg-slate-950/75' : ''}`}><nav className="mx-auto flex h-18 max-w-6xl items-center justify-between px-6" aria-label="Main navigation"><a href="#home" className="font-semibold tracking-tight text-ink dark:text-white">{profile.initials}<span className="text-accent">.</span></a><div className="hidden items-center gap-5 lg:flex">{links.map(link=><a className="nav-link" href={`#${link.toLowerCase()}`} key={link}>{link}</a>)}</div><button aria-label="Toggle menu" className="rounded-lg p-2 lg:hidden" onClick={()=>setOpen(!open)}>{open?<X size={20}/>:<Menu size={20}/>}</button></nav>{open&&<div className="border-t border-slate-200 bg-white px-6 py-5 dark:border-white/10 dark:bg-slate-950 lg:hidden">{links.map(link=><a onClick={()=>setOpen(false)} className="block py-2 text-sm" href={`#${link.toLowerCase()}`} key={link}>{link}</a>)}</div>}</header> }
